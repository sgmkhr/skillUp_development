
  <table class='table table-hover table-inverse'>
    <thead>
      <tr>
        <th></th>
        <th>Title</th>
        <th>Opinion</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
      <% @books.each do |book| %>
        <tr>
          <td><%= link_to user_path(book.attribute.user) do %>
            <%= image_tag book.attribute.user.get_profile_image, size:'50x50' %>
            <% end %>
          </td>
          <td><%= link_to book.attribute.title, book_path(book.id) %></td>
          <td><%= book.attribute.body %></td>
        </tr>
      <% end %>
      
      <% @users.each do |user| %>
        <% user.attribute.books.each do |book| %>
          <tr>
            <td><%= link_to user_path(book.attribute.user) do %>
              <%= image_tag book.attribute.user.get_profile_image, size:'50x50' %>
              <% end %>
            </td>
            <td><%= link_to book.attribute.title, book_path(book.attribute.id) %></td>
            <td><%= book.attribute.body %></td>
          </tr>
        <% end %>
      <% end %>
    </tbody>
  </table>
